#include <stdio.h>

void swap(int x*, int y*) {
    
}

int main() {
    int x = 100;
    int y = 200;

    printf("x = %d, y = %d", x ,y);
    swap();
    printf("x = %d, y = %d", x ,y);
    
    return 0;
}
